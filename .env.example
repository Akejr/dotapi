# Copy to .env.local and fill in.
#
# Where contact form submissions are forwarded. Accepts any endpoint that takes
# a JSON POST — Slack/Teams incoming webhooks, Zapier, Make, your own handler.
# Leave empty and submissions are only logged server-side; nothing is delivered.
CONTACT_WEBHOOK_URL=
